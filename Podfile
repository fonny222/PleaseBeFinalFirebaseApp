# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

pod 'Firebase/Core'
pod 'Firebase/Messaging'

target 'PleaseBeFinalFirebaseApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PleaseBeFinalFirebaseApp

  target 'PleaseBeFinalFirebaseAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PleaseBeFinalFirebaseAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

